# Lumen

Explorable webapp distilled from science YouTube videos. See `/Users/frederick/.claude/plans/delightful-wondering-cascade.md` for the design.

## Quickstart

```bash
uv sync
uv run python -m scripts.lib.db init
```

DB lands at `data/library.db`.
